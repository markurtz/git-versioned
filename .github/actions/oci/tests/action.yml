---
# Copyright 2026 markurtz
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
name: "OCI Tests"
description: "Runs OCI container structure tests (E2E) and no-op functional tests"
inputs:
  python-version:
    description: "Python version to set up for tooling"
    required: false
    default: "3.10"
  image-tar:
    description: "The path to a pre-built image tarball to load"
    required: true
  force-run:
    description: "Always run even if no changes are detected"
    required: false
    default: "false"
runs:
  using: "composite"
  steps:
    - name: Detect changes
      if: inputs.force-run != 'true'
      uses: dorny/paths-filter@d1c1ffe0248fe513906c8e24db8ea791d46f8590  # v3.0.3
      id: filter
      with:
        filters: .github/paths-filter.yml
    - name: Evaluate changes
      id: check
      shell: bash
      env:
        FORCE_RUN: ${{ inputs.force-run }}
        GITHUB_CHANGES: ${{ steps.filter.outputs.github_changes }}
        OCI_CHANGES: ${{ steps.filter.outputs.oci_changes }}
        SRC_CHANGES: ${{ steps.filter.outputs.src_changes }}
        PYPROJECT_CHANGES: ${{ steps.filter.outputs.pyproject_changes }}
      run: |
        if [[ "$FORCE_RUN" == "true" || \
              "$GITHUB_CHANGES" == "true" || \
              "$OCI_CHANGES" == "true" || \
              "$SRC_CHANGES" == "true" || \
              "$PYPROJECT_CHANGES" == "true" ]]; then
          echo "run=true" >> "$GITHUB_OUTPUT"
        else
          echo "run=false" >> "$GITHUB_OUTPUT"
        fi
    - name: Set up Python & Hatch
      if: steps.check.outputs.run == 'true'
      uses: ./.github/actions/utility/setup-python
      with:
        python-version: ${{ inputs.python-version }}
    - name: Set up OCI Tools
      if: steps.check.outputs.run == 'true'
      uses: ./.github/actions/utility/setup-oci
    - name: Run OCI E2E Tests
      if: steps.check.outputs.run == 'true'
      shell: bash
      env:
        IMAGE_TAR: ${{ inputs.image-tar }}
      run: |-
        if [ ! -f "$IMAGE_TAR" ]; then
          echo "[ERROR] Pre-built image tarball not found at: $IMAGE_TAR"
          exit 1
        fi
        echo "[INFO] Loading OCI image from $IMAGE_TAR..."
        docker load -i "$IMAGE_TAR"
        hatch run oci:tests-e2e
