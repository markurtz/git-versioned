---
# Copyright 2026 markurtz
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
name: "CI — Main"
on:
  push:
    branches:
      - main
concurrency:
  group: "main-${{ github.ref_name }}"
  cancel-in-progress: true
permissions:
  contents: write
  security-events: write
jobs:
  # ── Stage 1: Quality Gate ──────────────────────────────────────────────────
  project-quality:
    name: "Project Quality"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Run Project Quality
        uses: ./.github/actions/project/quality
  python-quality:
    name: "Python Quality (Py ${{ matrix.python-version }})"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.10"
          - "3.13"
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Run Python Quality
        uses: ./.github/actions/python/quality
        with:
          python-version: ${{ matrix.python-version }}
  oci-quality:
    name: "OCI Quality"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Run OCI Quality
        uses: ./.github/actions/oci/quality

  # ── Stage 3: Build ─────────────────────────────────────────────────────────
  python-build:
    name: "Python Build (Py ${{ matrix.python-version }})"
    needs:
      - python-quality
      - project-quality
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.10"
          - "3.13"
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Run Python Build
        uses: ./.github/actions/python/build
        with:
          python-version: ${{ matrix.python-version }}
      - name: Upload Build Artifact
        uses: actions/upload-artifact@b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882  # v4.4.3
        with:
          name: build-artifact-main-py${{ matrix.python-version }}-${{ github.run_id }}
          path: dist/
          retention-days: 14

  # ── Stage 4: Parallel Tests ────────────────────────────────────────────────
  project-functional-tests:
    name: "Project Functional Tests"
    needs:
      - project-quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Run Project Functional Tests
        uses: ./.github/actions/project/tests
        with:
          run-functional: "true"
          run-e2e: "false"
  project-e2e-tests:
    name: "Project E2E Tests (Links)"
    needs:
      - project-quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Run Project E2E Tests
        uses: ./.github/actions/project/tests
        with:
          run-functional: "false"
          run-e2e: "true"
  python-functional-tests:
    name: "Python Functional Tests (Py ${{ matrix.python-version }})"
    needs:
      - python-build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.10"
          - "3.13"
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Run Python Functional Tests
        id: run-tests
        uses: ./.github/actions/python/tests
        with:
          python-version: ${{ matrix.python-version }}
          run-functional: "true"
          run-e2e: "false"
          test-category: "smoke or sanity"
          generate-coverage: "true"
      - name: Upload Python Functional Coverage Report
        if: steps.run-tests.outputs.coverage-generated == 'true'
        uses: actions/upload-artifact@b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882  # v4.4.3
        with:
          name: coverage-report-functional-python-py${{ matrix.python-version }}-${{ github.run_id
            }}
          path: ${{ steps.run-tests.outputs.coverage-reports }}
          retention-days: 14
  python-e2e-tests:
    name: "Python E2E Tests (Py ${{ matrix.python-version }})"
    needs:
      - python-build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.10"
          - "3.13"
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Download Build Artifact
        uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372ec801d16  # v4.1.8
        with:
          name: build-artifact-main-py${{ matrix.python-version }}-${{ github.run_id }}
          path: dist/
      - name: Run Python E2E Tests
        id: run-tests
        uses: ./.github/actions/python/tests
        with:
          python-version: ${{ matrix.python-version }}
          run-functional: "false"
          run-e2e: "true"
          test-category: "smoke or sanity"
          generate-coverage: "true"
      - name: Upload Python E2E Coverage Report
        if: steps.run-tests.outputs.coverage-generated == 'true'
        uses: actions/upload-artifact@b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882  # v4.4.3
        with:
          name: coverage-report-e2e-python-py${{ matrix.python-version }}-${{ github.run_id
            }}
          path: ${{ steps.run-tests.outputs.coverage-reports }}
          retention-days: 14

  # ── Stage 5: Docs ──────────────────────────────────────────────────────────
  docs:
    name: "Build & Deploy Docs"
    needs:
      - project-quality
      - python-quality
      - oci-quality
      - project-functional-tests
      - project-e2e-tests
      - python-functional-tests
      - python-e2e-tests
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
        with:
          fetch-depth: 0
      - name: Run Project Docs Builder
        uses: ./.github/actions/project/docs
        with:
          build-type: "dev"
          alias: "latest"
          include-coverage: "true"
          deploy: "true"
