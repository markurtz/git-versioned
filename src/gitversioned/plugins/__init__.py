"""
Build system plugins for gitversioned.

This package provides the integration plugins required to connect the core
git-versioned logic with standard Python build backends, such as Hatchling
and Setuptools. These plugins serve as the entry points for the build systems
to query and inject the dynamically generated version strings into the
distribution metadata during the build process.
"""
